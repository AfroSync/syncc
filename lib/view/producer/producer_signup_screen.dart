import 'package:flutter/material.dart';

class ProducerSignupScreen extends StatelessWidget {
  const ProducerSignupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: []));
  }
}
